<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.firebase.example.fireeats">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".java.MainActivity"
            android:theme="@style/AppTheme.Activity"></activity>

        <activity
            android:name=".java.tuan.ShoppingProductGrid"
            android:theme="@style/AppTheme.Activity" />
        <activity android:name=".java.tuan.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".java.tuan.ShoppingCartSimple"
            android:screenOrientation="portrait" />
        <activity
            android:name=".java.tuan.ProfileFormActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".java.tuan.ShoppingProductAdvDetails"
            android:screenOrientation="portrait" />
        <activity
            android:name=".java.tuan.ShoppingCheckoutStep"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".java.tuan.ShoppingCheckoutTimeline"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".java.tuan.AboutAppSimple"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".java.tuan.BottomSheetMap"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".java.tuan.ArticleSimple"
            android:screenOrientation="portrait"></activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>